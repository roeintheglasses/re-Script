{
  "name": "@re-script/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and interfaces for re-script monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "files": [
    "dist",
    "src"
  ],
  "devDependencies": {
    "@re-script/tsconfig": "*",
    "typescript": "^5.5.4"
  },
  "publishConfig": {
    "access": "restricted"
  }
}